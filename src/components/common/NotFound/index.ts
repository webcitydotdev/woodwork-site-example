/**
 * @file NotFound component index
 * @description Export the NotFound component for easy imports
 */

import NotFound from './NotFound';
import type { NotFoundProps } from './NotFound.setup';

export { NotFoundProps };
export default NotFound;
